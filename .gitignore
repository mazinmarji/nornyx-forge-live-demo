.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.env
.env.*
!.env.example
.DS_Store
build/
dist/
*.egg-info/
# `.coverage` is written by `pytest --cov`, which `pyproject.toml`
# declares as a dev dependency. Untracked, it makes the session's
# governed-tree guard fail at teardown -- the guard is right, and the
# ordinary use of a declared dependency should not trip it.
.coverage
.coverage.*
coverage.xml
htmlcov/
.nornyx/runs/
.nornyx/cache/
# Everything under evidence/runtime/ is generated per run, including the
# per-mission nornyx/ subdirectory. The earlier globs matched only the top
# level, so subdirectory evidence could be committed by accident.
evidence/runtime/
evidence/build/*.json
generated/
worktrees/
.nornyx/architecture/*.json
.nornyx/security/*.json
.nornyx/requirements/
.nornyx/runtime/
.nornyx/foundation-decision.json
.nornyx/goals.json
.nornyx/demo-data.json

.nornyx/in-session/
.nornyx/generated/
