# Contributing

Run `python scripts/validate_repository.py`, `pytest`, and the Nornyx contract checks before proposing changes. Changes to governance contracts require explicit rationale and must not weaken existing denial or evidence requirements merely to make CI pass.
